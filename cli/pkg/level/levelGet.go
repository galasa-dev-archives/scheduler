/*
 * Copyright contributors to the Galasa project
 */
package level

import (
	"fmt"
)

func LevelGet(app string, currentName string, outputType string) {
	fmt.Println("what are LEVELs,  no idea :-)")
	fmt.Printf("get app=%s level=%s, type=%s\n", app, currentName, outputType)
}
